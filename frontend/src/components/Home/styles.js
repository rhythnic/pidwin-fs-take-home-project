import { theme } from "../../themes/Default";
import { deepPurple } from "@mui/material/colors";

export const styles = {
  form: {
    padding: "1rem",
    width: "calc(100% - 2rem)"
  },
  flipBtn: {
    marginTop: "1rem"
  }
};